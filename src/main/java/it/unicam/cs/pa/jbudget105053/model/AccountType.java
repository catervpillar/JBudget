package it.unicam.cs.pa.jbudget105053.model;

/**
 * This enumeration defines the two types an {@link Account} can be:
 * an {@code ASSET} or a {@code LIABILITY}.
 */
public enum AccountType {
    ASSET,
    LIABILITY
}
