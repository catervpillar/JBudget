package it.unicam.cs.pa.jbudget105053.model;

/**
 * This enumeration defines the two types a {@link Movement} can be:
 * an {@code INCREMENT} or a {@code DECREMENT}.
 */
public enum MovementType {
    INCREMENT,
    DECREMENT
}
